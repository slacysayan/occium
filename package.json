{
  "name": "occium-local",
  "private": true,
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "install:helper": "npm install --prefix local-helper",
    "start": "npm start --prefix frontend",
    "start:helper": "npm start --prefix local-helper",
    "build": "npm run build --prefix frontend"
  }
}
